package cn.com.startai.socket.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import cn.com.startai.socket.mutual.js.bean.DisplayBleDevice;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DISPLAY_BLE_DEVICE".
*/
public class DisplayBleDeviceDao extends AbstractDao<DisplayBleDevice, Long> {

    public static final String TABLENAME = "DISPLAY_BLE_DEVICE";

    /**
     * Properties of entity DisplayBleDevice.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property Address = new Property(2, String.class, "address", false, "ADDRESS");
        public final static Property Con = new Property(3, boolean.class, "con", false, "CON");
        public final static Property HasRemoteActivation = new Property(4, boolean.class, "hasRemoteActivation", false, "HAS_REMOTE_ACTIVATION");
        public final static Property Rssi = new Property(5, int.class, "rssi", false, "RSSI");
    }


    public DisplayBleDeviceDao(DaoConfig config) {
        super(config);
    }
    
    public DisplayBleDeviceDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DISPLAY_BLE_DEVICE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"NAME\" TEXT," + // 1: name
                "\"ADDRESS\" TEXT," + // 2: address
                "\"CON\" INTEGER NOT NULL ," + // 3: con
                "\"HAS_REMOTE_ACTIVATION\" INTEGER NOT NULL ," + // 4: hasRemoteActivation
                "\"RSSI\" INTEGER NOT NULL );"); // 5: rssi
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DISPLAY_BLE_DEVICE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, DisplayBleDevice entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String address = entity.getAddress();
        if (address != null) {
            stmt.bindString(3, address);
        }
        stmt.bindLong(4, entity.getCon() ? 1L: 0L);
        stmt.bindLong(5, entity.getHasRemoteActivation() ? 1L: 0L);
        stmt.bindLong(6, entity.getRssi());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, DisplayBleDevice entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String address = entity.getAddress();
        if (address != null) {
            stmt.bindString(3, address);
        }
        stmt.bindLong(4, entity.getCon() ? 1L: 0L);
        stmt.bindLong(5, entity.getHasRemoteActivation() ? 1L: 0L);
        stmt.bindLong(6, entity.getRssi());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public DisplayBleDevice readEntity(Cursor cursor, int offset) {
        DisplayBleDevice entity = new DisplayBleDevice( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // address
            cursor.getShort(offset + 3) != 0, // con
            cursor.getShort(offset + 4) != 0, // hasRemoteActivation
            cursor.getInt(offset + 5) // rssi
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, DisplayBleDevice entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setAddress(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setCon(cursor.getShort(offset + 3) != 0);
        entity.setHasRemoteActivation(cursor.getShort(offset + 4) != 0);
        entity.setRssi(cursor.getInt(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(DisplayBleDevice entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(DisplayBleDevice entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(DisplayBleDevice entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
